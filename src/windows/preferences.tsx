import React from "react";
import ReactDOM from "react-dom/client";
import Preferences from "@kianalol/components/Preferences";
import "@kianalol/styles/globals.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <Preferences />
  </React.StrictMode>
);
